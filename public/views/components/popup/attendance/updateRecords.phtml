<?php

global $APPLICATION, $ATTENDANCE_GROUP_BODY_KEY, $ATTENDANCE_GROUP_TABLE_HEADER_TEXT;

$control = $APPLICATION->FUNCTIONS->ATTENDANCE_GROUP_CONTROL;
$RECORDS = $control->getAllRecords(false);
$ID = "attendance_group_id";
$BUTTON = false;


echo CreateTable($ATTENDANCE_GROUP_TABLE_HEADER_TEXT, $ATTENDANCE_GROUP_BODY_KEY, $RECORDS, $ID, $BUTTON);
