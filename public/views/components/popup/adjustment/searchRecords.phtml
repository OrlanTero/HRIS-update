<?php
global $APPLICATION, $ADJUSTMENTS_TABLE_BODY_KEY, $ADJUSTMENTS_TABLE_HEADER_TEXT;

$search = $_POST['search'];

$control = $APPLICATION->FUNCTIONS->ADJUSTMENT_CONTROL;

$RECORDS = $control->searchRecords($search,false);
$ID = "adjustment_id";

echo CreateTable($ADJUSTMENTS_TABLE_HEADER_TEXT, $ADJUSTMENTS_TABLE_BODY_KEY, $RECORDS, $ID);
