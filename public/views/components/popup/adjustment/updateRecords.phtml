<?php

global $APPLICATION, $ADJUSTMENTS_TABLE_BODY_KEY, $ADJUSTMENTS_TABLE_HEADER_TEXT;

$control = $APPLICATION->FUNCTIONS->ADJUSTMENT_CONTROL;
$RECORDS = $control->getAllRecords(false);
$ID = "adjustment_id";
$BUTTON = false;

echo CreateTable($ADJUSTMENTS_TABLE_HEADER_TEXT, $ADJUSTMENTS_TABLE_BODY_KEY, $RECORDS, $ID, $BUTTON);
